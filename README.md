# laravel-vuetify-starterpack


Starter pack for Laravel with Vue + VueX + Vue-router + Vuetify

# Usage

1. Clone repo or install in .zip format

2. Run your console or cmd and write in project directory:

```
npm run watch
```

3. Run your twin console or cmd and write in project directory:

```
php artisan serve
```
